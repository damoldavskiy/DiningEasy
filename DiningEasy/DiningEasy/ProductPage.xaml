<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="DiningEasy.ProductPage">
    <ContentPage.Content>
        <StackLayout>
            <Image Aspect="AspectFill" Source="{Binding CurrentProduct.Path}"/>
            <StackLayout Margin="15,10,15">
                <Label FontSize="20" TextColor="Black" Text="{Binding CurrentProduct.Name}"/>
                <Label FontSize="14" TextColor="#333333" Text="{Binding CurrentProduct.Description}"/>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <Label FontSize="16" TextColor="Black" Text="Ингридиенты:"/>
                    <Label FontSize="13" TextColor="#333333" Text="{Binding CurrentProduct.Ingridients}"/>
                    <Label FontSize="16" TextColor="Black" FontAttributes="Bold" Text="{Binding CurrentProduct.Cost}"/>
                    <Label FontSize="16" TextColor="Black" FontAttributes="Italic" Text="{Binding CurrentProduct.Weight}"/>
                    <Button/>
                </Grid>
                </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>